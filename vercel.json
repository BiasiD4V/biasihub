{
  "version": 2,
  "buildCommand": "tsc && npm run build",
  "outputDirectory": "./dist",
  "env": {
    "BUILD_CMD": "tsc && vite build"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
